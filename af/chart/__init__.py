"""Charts: antigens, sera, titre tables and layers, column bases, projections.

`model` holds the data, `ace` reads and writes `.ace` files, `identity` says which
antigens/sera in two tables are the same, `merge` merges charts (the chains' layer merge),
`procrustes` fits one layout onto another.
"""
